name: Build and Tag

on:
  push:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📅 Build and Tag
        uses: ./build-tag
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          prefix: 'main'
          node-version: '18'
          create_changelog: true